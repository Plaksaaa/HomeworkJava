package homework.test;

public enum TestMenu {
    /*Автоматически предопределённый метод для перечисления values() возвращает массив,
    содержащий список констант перечисления.*/

    EXIT_COMMAND('0'),
    ADD_COMMAND('1'),
    GET_BY_KEY_COMMAND('2'),
    GET_ALL_COMMAND('3');

    TestMenu(char c) {
    }
}
